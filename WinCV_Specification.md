# WinCV 檔案管理系統 - 功能規格書

> **參考版本**: WinCV 0.52 2021.01.21 test  
> **文件用途**: 復刻開發參考規格

---

## 一、系統架構概覽

### 1.1 介面佈局

```
┌─────────────────────────────────────────────────────────────────┐
│ 標題列: WinCV 0.52 2021.01.21 test                              │
├─────────────────────────────────────────────────────────────────┤
│ 主選單: 1檔案 | 2目錄/磁碟 | 3網路 | 4標記 | 5檢視 | 6轉檔/轉碼/其他 │
│         7設定 | 8說明/註冊/更新版本                               │
├─────────────────────────────────────────────────────────────────┤
│ 工具列: [拷] [移] [改] [刪] [編] [執] [圖] [A] [V] [選單(F1)]     │
├─────────────────────────────────────────────────────────────────┤
│ 路徑列: C: [當前路徑顯示]                                        │
│ 路徑列: R: [參考路徑顯示]                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 檔案列表區域 (主要顯示區)                                         │
│ - 檔名 | 檔案大小 | 修改日期 | 修改時間                           │
│ - 顏色標示: 目錄(黃)、執行檔(綠)、壓縮檔(紅)、標記檔案(反白)        │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ 狀態列: 檔案數量統計 | 標記數量 | 磁碟剩餘空間                     │
├─────────────────────────────────────────────────────────────────┤
│ 下方面板: 選中檔案資訊 / 子目錄預覽                                │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 雙面板設計
- **C: (Current)**: 當前工作目錄
- **R: (Reference)**: 參考/目標目錄
- 支援在兩個面板間複製、移動檔案

---

## 二、主選單功能架構

### 2.1 【1檔案】選單 (Alt+1)

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| 拷貝/合併 | C (Copy) | 複製選中檔案到目標目錄，支援合併功能 |
| 刪除 | D (Delete) | 刪除選中檔案 |
| 移動 | M (Move) | 移動檔案到目標目錄 |
| 改名 | R (Rename) | 重新命名檔案 |
| 連續編號改名 | Alt+R | 批次依序編號重新命名 |
| 屬性 | A (Attrib) | 修改檔案屬性 (唯讀、隱藏、系統、封存) |
| 執行 | G (Go) | 執行選中的程式/檔案 |
| 比較檔案內容是否相同 | Alt+C | 比對兩個檔案的內容差異 |
| 內容、共用 | Ctrl+Shift+P | 查看/設定檔案內容與共用權限 |
| 註解 | Alt+E () | 為檔案添加註解說明 |
| 刪除註解 | Alt+D | 移除檔案的註解 |
| 編輯檔案 | E (Edit) | 開啟文字編輯器編輯檔案 |
| 編輯新檔案 | O (Open) | 建立並編輯新檔案 |
| 尋找列表字串 | F (Find) | 在檔案列表中搜尋字串 |
| 續找列表字串 | N (Next) | 繼續搜尋下一個符合項目 |
| 尋找 檔名/字串/註解 | W (Where) | 進階搜尋功能 |
| 16進位看檔 | H (Hex) | 以十六進位模式檢視檔案 |
| 強制以文字方式看檔 | V (View) | 強制以純文字模式開啟檔案 |
| 解壓縮檔 | unZip | 解壓縮 ZIP 等壓縮檔 |
| 壓縮檔案 | Alt+Z | 壓縮選中的檔案 |
| 結束程式 | Q (Quit) / ESC | 結束程式 (詢問確認) |
| 結束程式(不詢問) | Alt+X | 直接結束程式 |

---

### 2.2 【2目錄/磁碟】選單 (Alt+2)

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| 改變路徑 | P | 輸入路徑直接切換目錄 |
| 建子目錄 | F3 | 建立新資料夾 |
| 到根目錄 | \ | 快速跳至磁碟根目錄 |
| 目前路徑 內容、共用 | - | 查看當前目錄的內容與共用設定 |
| 磁碟內容、改變磁碟標籤 | L (Label) | 顯示磁碟資訊/修改磁碟標籤 |
| 重新讀入檔案列表 | F5 | 重新整理檔案列表 |
| 設定只列出特定檔案 | F8 | 設定檔案過濾條件 (如 *.txt) |
| 開啟檔案總管 | - | 開啟 Windows 檔案總管 |

---

### 2.3 【3網路】選單 (Alt+3)

| 功能名稱 | 說明 |
|---------|------|
| 將目前路徑設為連線網路磁碟機 | 將當前路徑映射為網路磁碟 |
| 連線網路磁碟機 | 連線到網路共用資源 |
| 中斷網路磁碟機 | 中斷網路磁碟連線 |
| 目前路徑 內容、共用 | 查看/設定當前路徑的共用設定 |

---

### 2.4 【4標記】選單 (Alt+4)

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| 標記 | 空白鍵 / 滑鼠中鍵 | 標記/取消標記單一檔案 |
| 標記所有檔案 | T (Tag all) | 標記當前目錄所有檔案 |
| 標記所有檔案及目錄 | Alt+T | 標記所有檔案與子目錄 |
| 解除所有標記 | U (Untag all) | 取消所有標記 |
| 批次標記檔案 | + | 依條件批次標記 (萬用字元) |
| 批次解除標記 | - | 依條件批次取消標記 |
| 標記變未標記(反向) | * | 反轉所有標記狀態 |

---

### 2.5 【5檢視】選單 (Alt+5)

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| (切換)以圖示(縮圖)方式列出檔案 | F12 | 切換清單/縮圖檢視模式 |
| 圖示(縮圖)大小 | > | 子選單：調整縮圖尺寸 |
| 繪圖背景顏色 | > | 子選單：設定背景顏色 |
| 刪除縮圖的快取檔 | - | 清除縮圖快取 |
| 設定縮圖快取存檔目錄 | - | 指定縮圖快取儲存位置 |
| 顯示縮圖(並建立)快取? | ✓ | 開關縮圖快取功能 |

#### 【5檢視】子選單詳細

**圖示(縮圖)大小**
| 選項 | 說明 |
|------|------|
| 大 | 最大縮圖尺寸 |
| 中 | 中等縮圖尺寸 |
| 中小 | 中小縮圖尺寸 |
| 小 | 最小縮圖尺寸 |
| 自設大小 | 自訂縮圖像素大小 |

**繪圖背景顏色**
| 選項 | 色碼建議 |
|------|----------|
| 黑色 | #000000 |
| 白色 | #FFFFFF |
| 灰色 | #808080 |
| 藍色 | #000080 |

---

### 2.6 【6轉檔/轉碼/其他】選單 (Alt+6)

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| 小算盤 | K | 開啟計算機 |
| 轉檔(簡繁內碼、圖檔格式、批次改檔名) | Ctrl+O | 批次轉換功能 |
| -- (可按T標記全部一次轉檔) | - | 提示：先標記再轉檔 |
| 使視窗進入待機狀態 | - | 最小化到系統匣 |
| 視窗抓圖 | Ctrl+Alt+S | 螢幕截圖功能 |

#### 檔案校驗功能群組

| 功能名稱 | 說明 |
|---------|------|
| 使用.sfv檢驗 | 使用 SFV 檔驗證檔案完整性 |
| 產生.sfv檔 (請先標記檔案) | 產生 SFV 校驗檔 |
| 計算crc32 | 計算檔案 CRC32 值 |
| 檢驗crc32 | 驗證 CRC32 |
| 使用.md5檢驗 | 使用 MD5 檔驗證檔案完整性 |
| 產生.md5檔 (請先標記檔案) | 產生 MD5 校驗檔 |
| 計算md5 | 計算檔案 MD5 值 |
| 檢驗md5 | 驗證 MD5 |

---

### 2.7 【7設定】選單 (Alt+7)

#### 顯示設定

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| 顏色 | > | 子選單：自訂色彩主題 |
| 字型大小 | > | 子選單：調整字體/視窗大小 |
| 檔名顯示語系 | > | 子選單：設定檔名編碼 |
| 是否顯示選單 | ✓ | 開關主選單顯示 |
| 顯示按鈕? | ✓ | 開關工具列按鈕 |
| 全螢幕顯示 | F11 | 切換全螢幕模式 |

#### 檔案列表設定

| 功能名稱 | 快捷鍵 | 說明 |
|---------|--------|------|
| 設定 預設註解檔檔名 | - | 設定註解檔的預設名稱 |
| 檔案列表不列出dir.doc? | ✓ | 是否隱藏 dir.doc |
| 自動記憶光棒在各目錄的位置? | ✓ | 記住游標位置 |
| 預視檔案選項 | > | 子選單：預覽設定 |
| 改變列表排序方式 | S (Sort) | 變更排序方式 |
| 檔名顯示選項 | > | 子選單：檔名顯示格式 |
| 檔案列表要列出磁碟機? | - | 是否顯示磁碟機清單 |

#### 程式行為設定

| 功能名稱 | 說明 |
|---------|------|
| 如設定離開要詢問則不常駐 | 設定離開行為 |
| 按ESC或最小化時常駐右下角而不離開? | 最小化到系統匣 (Alt+↑↓ 回復) |
| 使用alt-↑↓回復wincv? | ✓ 熱鍵回復視窗 |
| 處理檔案時，允許常駐右下角? | ✓ 背景處理設定 |
| 離開WinCV時要詢問? | 結束確認設定 |

#### 刪除設定

| 功能名稱 | 說明 |
|---------|------|
| 刪除檔案要放到資源回收桶? | ✓ 使用資源回收桶 |
| 若不放到資源回收桶，則使用填0方式刪除? | 安全刪除 (填0覆寫) |

#### 看圖/覆蓋設定

| 功能名稱 | 說明 |
|---------|------|
| 看圖、存圖選項 | > 子選單 |
| Shift+上、下 與CVIEW for DOS相同功能? | 相容舊版操作 |
| 自動覆蓋內容完全相同的檔案? | ✓ 自動覆蓋相同檔案 |
| 自動覆蓋時內容相同檔案時，不考慮檔案日期? | ✓ 忽略日期比對 |
| 設定 全螢幕抓圖預設存檔路徑 | 截圖存檔位置 |

#### 其他設定

| 功能名稱 | 說明 |
|---------|------|
| 清除曾經輸入的字串記錄 | 清除輸入歷史 |
| 結束程式時，自動清除輸入字串記錄? | 自動清除歷史 |
| 進入文字編輯器時，隱藏wincv主視窗? | 編輯時隱藏主視窗 |
| 降低Copy檔案速度? | 降低複製速度 (減少系統負載) |

---

### 2.8 【7設定】子選單詳細規格

#### 顏色主題 (Color Themes)

| 主題名稱 | 背景色 | 文字色 | 色碼建議 |
|---------|--------|--------|----------|
| 黑底灰字 | 黑色 | 灰色 | bg:#000000, fg:#808080 |
| 黑底柔白字 | 黑色 | 柔白 | bg:#000000, fg:#E0E0E0 |
| 藍底灰字 | 深藍 | 灰色 | bg:#000080, fg:#808080 |
| 灰底黑字 | 灰色 | 黑色 | bg:#808080, fg:#000000 |
| 白底黑字 | 白色 | 黑色 | bg:#FFFFFF, fg:#000000 |
| 褐底黑字 | 褐色 | 黑色 | bg:#8B4513, fg:#000000 |
| 褐底白字 | 褐色 | 白色 | bg:#8B4513, fg:#FFFFFF |
| 青底黑字 | 青色 | 黑色 | bg:#008080, fg:#000000 |

#### 字型大小 / 視窗尺寸

| 選項 | 解析度 | 說明 |
|------|--------|------|
| 小 | 640×480 | 適合小螢幕/低解析度 |
| 中 | 800×600 | 標準尺寸 |
| 大 | 1280×1024 | 適合高解析度螢幕 |

#### 檔名顯示語系

| 選項 | 說明 |
|------|------|
| 記憶語系設定? | 記住上次選擇的語系 |
| 繁體中文 | Big5 編碼 (Traditional Chinese) |
| 簡體中文 | GB2312/GBK 編碼 (Simplified Chinese) |
| 日文 | Shift-JIS 編碼 (Japanese) |
| 韓文 | EUC-KR 編碼 (Korean) |

#### 預視檔案選項

| 選項 | 快捷鍵 | 說明 |
|------|--------|------|
| 調整 磁碟視窗、預視視窗 的大小 | - | 拖曳調整面板大小 |
| 切換 是否顯示預視視窗 | Alt+P | 開關預覽面板 |
| 預視圖檔? | ✓ | 預覽圖片檔案 |
| 預視.avi基本資料? | - | 預覽影片檔基本資訊 |
| 預視壓縮檔 列表? | ✓ | 顯示壓縮檔內容清單 |
| 預視壓縮檔 主註解? | ✓ | 顯示壓縮檔的註解 |
| A磁碟不預視? | - | 跳過軟碟機預覽 (避免讀取延遲) |
| 自動辨識簡體文字檔? | - | 自動偵測簡體中文編碼 |

#### 檔名顯示選項

| 選項 | 預設 | 說明 |
|------|------|------|
| 顯示 長度? | ✓ | 顯示檔案大小欄位 |
| 顯示 日期? | ✓ | 顯示修改日期欄位 |
| 顯示 時間? | ✓ | 顯示修改時間欄位 |
| 顯示 註解? | - | 顯示檔案註解欄位 |
| 以 Kb 方式顯示檔案大小? | ✓ | 用 KB 而非 Bytes 顯示 |
| 以長檔名方式顯示檔名? | ✓ | 顯示完整長檔名 (非 8.3 格式) |

#### 看圖、存圖選項

| 選項 | 說明 |
|------|------|
| 看圖用PgUp PgDn看上(下)一圖? | ✓ = 切換上下張圖片；否 = 翻頁瀏覽 |
| 預設將圖縮成當幕可容納? | 自動縮放至螢幕可完整顯示 |
| 預設將圖縮成當幕寬度可容納? | 自動縮放至螢幕寬度 |
| 按 ENTER 離開看圖? | ✓ = Enter 關閉檢視；否 = Enter 看下一張 |
| 看圖縮放時使用較佳品質? | 高品質縮放 (限 XP/NT，較慢) |
| Jpg 存檔畫質 | 設定 JPEG 儲存品質 (1-100) |

---

### 2.9 【8說明/註冊/更新版本】選單 (Alt+8)

| 功能名稱 | 說明 |
|---------|------|
| 說明 | 開啟使用說明 |
| 註冊 | 軟體註冊功能 |
| 更新版本 | 檢查/下載更新 |

---

## 三、核心功能模組詳細規格

### 3.1 檔案操作模組

#### 3.1.1 複製功能 (Copy)
```
輸入：
- 來源檔案/目錄列表 (支援多選/標記)
- 目標路徑

行為：
- 支援單檔/多檔/目錄複製
- 支援檔案合併 (多檔合併為一檔)
- 顯示複製進度
- 衝突處理：覆蓋/跳過/重新命名/取消
- 支援「自動覆蓋相同內容檔案」選項

輸出：
- 複製結果統計
- 錯誤報告
```

#### 3.1.2 移動功能 (Move)
```
輸入：
- 來源檔案/目錄列表
- 目標路徑

行為：
- 同磁碟：直接移動 (改名方式)
- 跨磁碟：複製後刪除
- 衝突處理同複製功能

輸出：
- 移動結果統計
```

#### 3.1.3 刪除功能 (Delete)
```
輸入：
- 檔案/目錄列表

行為：
- 確認對話框 (可設定關閉)
- 支援送至資源回收桶
- 支援安全刪除 (填0覆寫)
- 顯示刪除進度

設定選項：
- 是否送入資源回收桶
- 是否使用填0刪除
```

#### 3.1.4 重新命名功能 (Rename)
```
單檔改名：
- 輸入新檔名
- 驗證檔名合法性
- 處理重複命名

批次編號改名 (Alt+R)：
- 設定前綴/後綴
- 設定起始編號
- 設定編號位數
- 設定間隔數
- 預覽重新命名結果
```

---

### 3.2 檔案瀏覽模組

#### 3.2.1 列表檢視模式
```
顯示欄位：
- 檔案名稱
- 檔案大小 (bytes)
- 修改日期 (MM-DD-YY)
- 修改時間 (HH:MM)

顏色編碼：
- 目錄：黃色
- 執行檔 (.exe, .com, .bat)：綠色
- 壓縮檔 (.zip, .rar, .7z)：紅色
- 被標記項目：反白顯示
- 一般檔案：白色

排序選項：
- 依檔名
- 依副檔名
- 依大小
- 依日期
- 昇冪/降冪切換
```

#### 3.2.2 縮圖檢視模式 (F12)
```
功能：
- 圖片檔案顯示縮圖
- 可調整縮圖大小
- 可設定背景顏色
- 縮圖快取機制
  - 可設定快取目錄
  - 可清除快取
  - 可開關快取功能
```

#### 3.2.3 檔案預覽
```
支援格式：
- 文字檔：直接預覽內容
- 圖片檔：縮圖預覽
- 16進位檢視 (Hex)
- 強制文字檢視 (View)
```

---

### 3.3 搜尋功能模組

#### 3.3.1 列表內搜尋 (Find/Next)
```
功能：
- 在當前檔案列表中搜尋字串
- F (Find)：開始搜尋
- N (Next)：繼續找下一個
- 高亮顯示符合項目
```

#### 3.3.2 進階搜尋 (Where)
```
搜尋範圍：
- 檔案名稱
- 檔案內容字串
- 檔案註解

搜尋選項：
- 大小寫敏感
- 萬用字元支援 (*, ?)
- 子目錄遞迴搜尋
```

---

### 3.4 標記系統模組

#### 3.4.1 標記機制
```
標記方式：
- 空白鍵：切換單一項目標記狀態
- 滑鼠中鍵：切換單一項目標記狀態

視覺呈現：
- 已標記：反白顯示
- 未標記：一般顯示

統計顯示：
- 狀態列顯示已標記數量
- 顯示已標記檔案總大小
```

#### 3.4.2 批次標記
```
功能：
- Tag all (T)：標記所有檔案
- Alt+T：標記所有檔案及目錄
- Untag all (U)：取消所有標記
- + 號：批次標記 (支援 *.txt 等萬用字元)
- - 號：批次取消標記
- * 號：反轉所有標記狀態
```

---

### 3.5 檔案校驗模組

#### 3.5.1 CRC32 功能
```
功能：
- 計算單一/多個檔案的 CRC32
- 驗證 CRC32 值

檔案格式：
- .sfv 檔案格式支援
```

#### 3.5.2 MD5 功能
```
功能：
- 計算單一/多個檔案的 MD5
- 驗證 MD5 值

檔案格式：
- .md5 檔案格式支援
```

---

### 3.6 壓縮功能模組

#### 3.6.1 解壓縮 (unZip)
```
支援格式：
- ZIP
- 其他常見壓縮格式

功能：
- 解壓到當前目錄
- 解壓到指定目錄
- 解壓特定檔案
```

#### 3.6.2 壓縮 (Alt+Z)
```
功能：
- 壓縮標記的檔案
- 設定壓縮檔名
- 設定壓縮等級
```

---

### 3.7 註解系統模組

#### 3.7.1 檔案註解
```
功能：
- 為檔案添加文字註解
- 編輯現有註解
- 刪除註解
- 在搜尋中包含註解

儲存方式：
- 註解檔 (預設檔名可設定)
- 與目錄關聯
```

---

### 3.8 網路功能模組

#### 3.8.1 網路磁碟機
```
功能：
- 連線網路磁碟機
- 中斷網路磁碟機
- 將當前路徑設為網路磁碟機

支援：
- Windows 網路芳鄰 (SMB)
- UNC 路徑
```

---

### 3.9 轉檔功能模組

#### 3.9.1 批次轉檔 (Ctrl+O)
```
支援類型：
- 簡繁內碼轉換 (Big5 ↔ GB)
- 圖檔格式轉換
- 批次改檔名

工作流程：
- 先標記檔案 (T)
- 開啟轉檔對話框
- 設定轉換參數
- 執行轉換
```

---

## 四、快捷鍵對照表

### 4.1 全域快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| F1 | 開啟選單 |
| F3 | 建立子目錄 |
| F5 | 重新整理 |
| F8 | 設定檔案過濾 |
| F11 | 全螢幕 |
| F12 | 切換縮圖模式 |
| ESC | 結束程式 (詢問) |
| \ | 到根目錄 |
| Space | 標記/取消標記 |

### 4.2 檔案操作快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| C | 複製 |
| D | 刪除 |
| M | 移動 |
| R | 改名 |
| E | 編輯 |
| G | 執行 |
| V | 文字檢視 |
| H | 16進位檢視 |
| Alt+R | 連續編號改名 |
| Alt+C | 比較檔案 |
| Alt+E | 編輯註解 |
| Alt+D | 刪除註解 |
| Alt+Z | 壓縮 |

### 4.3 標記快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| T | 標記全部檔案 |
| Alt+T | 標記全部檔案及目錄 |
| U | 取消所有標記 |
| + | 批次標記 |
| - | 批次取消標記 |
| * | 反轉標記 |

### 4.4 搜尋快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| F | 搜尋 |
| N | 繼續搜尋 |
| W | 進階搜尋 |

### 4.5 其他快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| P | 改變路徑 |
| L | 磁碟標籤 |
| K | 小算盤 |
| S | 排序 |
| Ctrl+O | 轉檔 |
| Ctrl+Shift+P | 內容/共用 |
| Ctrl+Alt+S | 視窗抓圖 |
| Alt+X | 直接結束 |
| Alt+↑↓ | 回復視窗 |

---

## 五、狀態列資訊

### 5.1 顯示內容
```
格式: [檔案數] / [總檔案數] 標記: [標記數] / [標記大小] / [總大小]

範例: 7 / 27 標記: 0 / 0 / 35,967,126
```

### 5.2 底部面板
```
顯示內容：
- 選中檔案名稱
- 檔案類型 (<DIR> 表示目錄)
- 檔案大小
- 修改日期時間
- 剩餘空間: [已用] / [總容量]

範例: 
FSCapture.exe    7,701,440 11-28-25 10:59  剩餘: 1,134,764MB / 1,906,610MB
```

---

## 六、設定檔結構建議

### 6.1 使用者偏好設定
```json
{
  "display": {
    "showMenu": true,
    "showToolbar": true,
    "colorTheme": "blackGray",
    "windowSize": "medium",
    "fullscreen": false,
    "colors": {
      "theme": "黑底灰字",
      "background": "#000000",
      "foreground": "#808080",
      "directory": "#FFFF00",
      "executable": "#00FF00",
      "archive": "#FF0000",
      "selected": "#FFFFFF"
    }
  },
  "fontSize": {
    "preset": "medium",
    "resolution": "800x600"
  },
  "fileNameEncoding": {
    "rememberSetting": true,
    "language": "繁體中文",
    "encoding": "Big5"
  },
  "fileList": {
    "showLength": true,
    "showDate": true,
    "showTime": true,
    "showAnnotation": false,
    "sizeInKb": true,
    "longFileName": true,
    "hideDirDoc": true,
    "showDriveList": false,
    "rememberCursorPosition": true
  },
  "preview": {
    "enabled": true,
    "showPreviewPane": true,
    "previewImages": true,
    "previewAviInfo": false,
    "previewArchiveList": true,
    "previewArchiveComment": true,
    "skipFloppyDrive": false,
    "autoDetectSimplifiedChinese": false
  },
  "thumbnail": {
    "enabled": true,
    "size": "medium",
    "sizeOptions": ["大", "中", "中小", "小", "自設大小"],
    "customSize": null,
    "backgroundColor": "黑色",
    "cacheEnabled": true,
    "cacheDirectory": ""
  },
  "imageViewer": {
    "pgUpDownSwitchImage": true,
    "fitToScreen": true,
    "fitToWidth": false,
    "enterToExit": true,
    "highQualityScaling": false,
    "jpegQuality": 85
  },
  "behavior": {
    "confirmOnExit": true,
    "minimizeToTray": false,
    "useAltArrowToRestore": true,
    "allowTrayWhileProcessing": true,
    "useTrashBin": true,
    "secureDelete": false,
    "autoOverwriteIdentical": true,
    "ignoreFileDateOnOverwrite": true,
    "reduceCopySpeed": false,
    "shiftUpDownLegacyMode": false
  },
  "history": {
    "clearOnExit": false
  },
  "editor": {
    "hideMainWindowOnEdit": false
  },
  "paths": {
    "screenshotSavePath": "",
    "defaultAnnotationFile": "dir.doc"
  }
}
```

---

## 七、檔案類型顏色定義

| 類型 | 顏色代碼 | 說明 |
|------|----------|------|
| 目錄 | Yellow (#FFFF00) | 資料夾 |
| 執行檔 | Green (#00FF00) | .exe, .com, .bat 等 |
| 壓縮檔 | Red (#FF0000) | .zip, .rar, .7z 等 |
| 系統檔/隱藏 | Gray (#808080) | System Volume Information 等 |
| 一般檔案 | White (#FFFFFF) | 其他檔案 |
| 已標記 | 反白 (Inverted) | 被標記的項目 |
| 特殊標示 | Cyan (#00FFFF) | 新修改/特殊狀態 |

---

## 八、開發優先順序建議

### Phase 1: 核心功能 (MVP)
1. 檔案列表顯示 (含顏色標示)
2. 目錄導航 (進入/返回/跳轉)
3. 基本檔案操作 (複製/移動/刪除/改名)
4. 標記系統
5. 快捷鍵支援

### Phase 2: 進階功能
1. 搜尋功能
2. 排序功能
3. 檔案預覽
4. 壓縮/解壓縮
5. 批次改名

### Phase 3: 擴充功能
1. 縮圖模式
2. 註解系統
3. 檔案校驗 (CRC32/MD5)
4. 網路磁碟機
5. 轉檔功能

### Phase 4: 設定與最佳化
1. 完整設定系統
2. 效能最佳化
3. 多語系支援
4. 主題/外觀自訂

---

## 九、技術實作建議

### 9.1 建議技術棧
- **前端框架**: Electron + React/Vue (跨平台桌面應用)
- **或**: Tauri + Rust (輕量高效)
- **或**: Qt + C++ (傳統但效能極佳)

### 9.2 關鍵元件
- 虛擬列表 (Virtual List) - 處理大量檔案
- 檔案系統 API 封裝
- 熱鍵管理器
- 設定持久化

---

*文件版本: 1.0*  
*建立日期: 2025-12-31*
